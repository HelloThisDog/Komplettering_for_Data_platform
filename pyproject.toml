[project]
name = "komplettering_dataplatform"
version = "0.1.0"
description = "can't be bothered to add one"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.8",
    "psycopg[binary,pool]>=3.2.13",
    "python-dotenv>=1.2.1",
    "kafka-python>=2.3.1",
]
